# 변형: 자유 회전 핸들

## UI 요소

### 위치
- 선택 영역/레이어 바운딩 박스 모서리

### 시각적 피드백
- 모서리를 벗어난 회전 핸들 표시
- 드래그 시 자유 각도 회전
- `Shift` 키로 15도 단위 스냅

### 상호작용
1. 변형 모드 활성화
2. 회전 핸들 드래그 → 자유 회전
3. `Enter` → 확정

### 접근성
- `debug_id`: transform-free-rotate
