# 캔버스 자르기 드래그

## UI 요소

### 동작
- 자르기 도구로 영역을 지정하여 드래그

### 시각적 피드백
- 자르기 영역 외부가 어둡게 표시
- 영역 모서리/가장자리에 핸들 표시
- 가로세로 비율 표시

### 상호작용
1. 자르기 도구 활성화
2. 드래그 → 자르기 영역 지정
3. 핸들 드래그 → 영역 조정
4. `Enter` → 자르기 적용

### 접근성
- `debug_id`: canvas-crop-drag-control
