# 캔버스 줌 스크롤

## UI 요소

### 동작
- 마우스 휠 또는 `Ctrl` + `+`/`-`

### 시각적 피드백
- 확대/축소가 마우스 커서 위치를 중심으로 적용
- 줌 레벨 표시 (예: 100%, 200%)

### 상호작용
1. `Ctrl` + 휠 위로 → 확대
2. `Ctrl` + 휠 아래로 → 축소

### 접근성
- `debug_id`: canvas-zoom-scroll
