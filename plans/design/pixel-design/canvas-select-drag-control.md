# 캔버스 선택 영역 드래그

## UI 요소

### 동작
- 선택 도구로 캔버스에서 드래그

### 시각적 피드백
- 점선 마치로 선택 영역 표시
- 선택 영역 외부가 반투명 오버레이로 어둡게 표시

### 상호작용
1. 선택 도구 활성화
2. 드래그 → 사각형 선택 영역 생성
3. 선택 영역 내부에서만 편집 가능

### 접근성
- `debug_id`: canvas-select-drag-control
