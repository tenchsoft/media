# Player

Product app slot for video playback, subtitles, playlists, and AI media analysis.

Primary plan source: `~/docs/plans/player`.

Expected shared foundations:

- `packages/app-shell`
- `packages/engine-client`
- `crates/media-core`
- `crates/search-core`
- `crates/storage-core`

The plan directory and product name use `player`. Keep this directory aligned
with the plan source unless the product name is explicitly renamed.
